{
  "name": "my-app",
  "type": "module",
  "scripts": {
  "dev": "bun --watch src/index.ts",
  "start": "bun x prisma generate && bun x prisma db push && bun run src/index.ts",
    "deploy": "wrangler deploy --minify",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings"
  },
  "dependencies": {
    "@prisma/adapter-pg": "^7.8.0",
    "@prisma/client": "^7.8.0",
    "dotenv": "^17.4.2",
    "hono": "^4.12.23"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "@types/node": "^25.9.2",
    "prisma": "^7.8.0",
    "wrangler": "^4.4.0"
  }
}